<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <span class="navbar-brand" href="#">Admin Panel</span>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/admin/products/list">Products <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="/admin/category/list">Categories <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="/admin/password">Change Password<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="/logout">Logout<span class="sr-only">(current)</span></a>
            </li>
        </ul>

        <form id="search-form" class="form-inline my-2 my-lg-0" action="" method="POST" <%if(!locals.search){ %> hidden
            <% } %> enctype="application/x-www-form-urlencoded">
            <select id="search-select" name="category" class="form-control mr-sm-2" placeholder="" aria-label=""
                value="<%= locals.active_category %>" <% if(!locals.search_category){ %> hidden <% } %>>
                <option value="nothing"></option>
            </select>
            <input class="form-control mr-sm-2" name="searcht" type="search" placeholder="Search"
                value="<%= locals.search_text %>" aria-label="Search" />
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">
                Search
            </button>
        </form>
    </div>
</nav>